# WeatherAggregator Environment Configuration
# No secrets required - all weather APIs are free/unauthenticated

# Optional: Set NODE_ENV (defaults to production in docker-compose.prod.yml)
NODE_ENV=production
